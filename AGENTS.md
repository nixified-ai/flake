# Agent Instructions

- Always run `nix fmt` in the repository root before concluding work on any task.
- Never propose or introduce insecure configuration solutions (e.g. passwordless root logins, disabling security constraints), except as temporary troubleshooting changes which must be quickly reverted.

